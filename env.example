# Supabase Configuration
# These are the keys you'll find in your Supabase dashboard under Settings > API

# PUBLISHABLE KEY (Anon Key) - Safe for client-side, used in browser
# This key is safe to expose in your frontend code
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your_publishable_anon_key_here

# SECRET KEY (Service Role Key) - Server-side only, never expose to client
# This key has admin privileges and should only be used in server routes
SUPABASE_SERVICE_ROLE_KEY=your_secret_service_role_key_here

# Optional: Database URL for direct connections (if needed)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres 